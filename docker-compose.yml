services:
  bixuetang:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - bixuetang-data:/data
    environment:
      # HTTPS 反代部署时改为 "1"
      COOKIE_SECURE: "0"
    restart: unless-stopped

volumes:
  bixuetang-data:

# 更新日志

本项目的所有重要变更都记录在此。格式参考
[Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/),
版本遵循[语义化版本](https://semver.org/lang/zh-CN/)。

发布流程见 README「发布与更新」一节:`scripts/release.sh` 会把下面的
`[未发布]` 内容定版、打 tag,CI 据此出镜像并生成 GitHub Release。

## [未发布]

## [0.3.0] - 2026-08-09

### 新增
- 邮箱验证:绑定/更改找回邮箱后不再直接生效——先发确认链接到该邮箱,点了才算
  验证通过(24 小时有效),只有验证过的邮箱能用于找回密码。设置页显示「已验证/
  未验证」并可「重新发送验证邮件」;注册仍先放行(邮箱标未验证,验证后才能找回)。
  堵住「绑一个不属于自己的邮箱来劫持找回」的漏洞。历史已绑邮箱一律视为已验证
  (迁移 0038 grandfather),不影响老用户。
- 好友关系更清楚:好友榜给回关了你的人标「互关」;新增「关注我的」列表,
  列出关注你但你还没回关的人,一键「回关」变互相关注。关注仍是单向,只是把
  单向/互相的状态摆明。

### 修复
- 邀请/分享链接不再明文暴露用户 id:`?ref=<自增id>` 改成 `?ref=<base36(id)~HMAC签名>`,
  服务端验签解码归因,别人拿不到 id、也难伪造指向任意用户。签名密钥走 `REF_SECRET`
  环境变量(不设用弱默认)。
- 邀请图/分享图的 bilibili 头像画不出:hdslb 图床无 CORS 头,canvas 跨域取不到 →
  新增同源头像代理 `/api/avatar/[id]`,bilibili 头像经它同源吐给 canvas 就能画。
- 成为 X 路线总览页给已有目标的老用户加「告诉我们你想要的路线」入口——之前反馈
  职业路线只在新手引导里有,老用户没处提。提交只记建议、不动其当前目标。
- 地图顶部「换目标」从行尾小字改成独立可点胶囊(更大更好按),并直接跳路线总览页
  (在那儿换成别的目标),不再只是链到当前目标详情。
- 找回密码:发信失败(SMTP 配错/网络)不再让整个请求 500、把用户甩到错误页;
  改为吞掉异常并记服务器日志,对用户一律回同样的成功文案——顺带堵上原先
  「注册邮箱报错、未注册邮箱正常」会泄露账号是否存在的漏洞。

## [0.2.0] - 2026-08-09

### 新增
- 课程学习心得(UGC):每门课页新增「学习心得」——过来人留一句短评/建议(先看哪几集、
  配合什么练习、哪里容易卡),别人看得到,帮后来的人少走弯路。发布走敏感词过滤、
  只能删自己的。管理端新增「课程心得」页,运营可下架任意漏网/不当内容(敏感词库
  只拦已收录的词,人工审核兜底;下架二次确认、不可撤销)。
- 学习提醒(Web Push,设置页开关):把必学堂装成 App 后可开启推送——复习卡到期时
  提醒你回来清卡,别让记忆掉线。VAPID 密钥走环境变量(不配则功能自动关闭);
  回访推送由 `pnpm push:reminders` 定时脚本发出(挂 cron)。
- 自习室(在「好友」页入口):进一间自习室,看到此刻还有谁在一起学(头像 + 已自习
  几分钟),有人陪着更学得下去。进入即登记在场、每 20 秒心跳同步、离开或关页即退出;
  可自建自习室(名称走敏感词过滤)。
- 好友与社交(底部新增「好友」Tab):
  - **关注/好友**——按用户名或昵称搜人一键关注;好友 = 你 + 关注的人。
  - **好友榜**——好友按总 XP 排行,带等级与 PK 段位,自己高亮,冠军戴皇冠。
  - **邀请闭环**——邀请卡生成带你头像 + 二维码的邀请图(复用分享图逻辑)或复制链接;
    好友经邀请注册后与你自动互相关注,等 TA 完成新手引导,你得 +50 XP、+100 金币(幂等防刷)。
- 目标选「其他」:目标选择里新增「其他（我想成为…）」——写下你想成为的角色,一键
  提交给运营考虑新增对应「成为 X」路线,同时进入「暂时随便逛逛」(目标留空、冒险
  路线随便选),地图顶部显示「暂时随便逛逛」。管理端新增「职业建议」页收集这些反馈。
- 登录时序防探测的"假"哈希改为运行时现算并缓存(不再内联密文字面量),行为与
  时序不变,顺带消除密钥扫描器(GitGuardian)对该常量的误报,两处登录去重。
- 防刷限流:登录态写操作(课程心得、职业建议、建自习室、关注)加每用户限流
  (如心得 8 条/时、建自习室 5 间/天),挡机器人刷屏爆库,正常用户无感。匿名端点
  (注册/登录/找回)建议反代按 IP 限速——已写进 README「防刷与限流」。
- 部署配置向导:`scripts/deploy.sh` 支持交互式配置——缺必填(域名等)时自动弹向导,
  也可 `--config` 单跑或 `--reconfigure` 逐项复核。能自动生成 VAPID 密钥、密码类
  输入不回显、已填项回车保留、可选功能(推送/邮件)按需跳过;配置写进 `.env`。
  `.env.example` 同步补齐生产可选项。
- 字数上限收口:补齐注册/B 站建号的昵称长度上限(32),防超长文本灌库;全站
  能存文本处(心得 300、职业建议 100、自习室名 20、视频反馈 500、成效小结 2000、
  视频笔记 8000、昵称 32)均已有上限。
- 敏感词过滤:昵称(注册/设置)、视频失效反馈、职业建议等用户输入接入敏感词过滤,
  命中即拒绝并提示;能识破插空/全角/大小写等常见规避。底库用开源综合词库
  (约 4 万词),匹配用「输入子串查表」,词库多大都不拖慢;业务词在
  `src/lib/moderation/words.ts` 增删。
- 「成为 X」加职业图标:路线列表 / 详情页 / 地图目标条都给每条职业路线配了图标 +
  专属配色(AI🧠、全栈🌐、游戏🎮、安全🛡️、数据📊、后端⚙️),更有辨识度。
- 每日任务栏:折叠态在图标上加红色角标,显示还有几个任务没搞定,不点开也一眼看到。
- 理顺「目标」与「冒险路线」的关系(目标为纲,路线跟随):地图顶部新增一条关系条——
  上行是长期目标(成为 X,可「换目标」),下行是此刻在爬的线。设了目标默认「跟随目标」:
  地图自动跟着目标当前该学的那条线走、随进度前移;你仍可「换路线」临时去别的线探索,
  这时给一个「回到目标路线」把你拉回。没设目标就只显示「当前路线」+「设定目标 →」。
  推荐卡不再重复显示目标,只管「接着学什么」。
- 目标可更换(带「不忘初心」提示):路线页可把某条「成为 X」设为目标 / 取消目标;
  已有目标时切换前先弹确认——劝你守住初心,确认无误再切,换后按新路线重排「下一步」。
- 老用户补弹目标提示:引导上线前就注册、还没定目标的用户,进地图弹一次介绍「成为 X」
  并让其挑一个(或「以后再说」),只弹一次。
- 地图每日任务栏改低调:默认折叠成右下角小药丸,不再每次进地图都弹开挡视野;
  展开/折叠的选择记在本地,回来还是你上次的样子。
- 登录支持用户名或邮箱:账号密码登录时,凭据填用户名或注册邮箱都行(带 @ 按邮箱
  查,否则按用户名),不再强制记用户名。
- 密码找回:注册可选填邮箱、设置页可随时绑定/更换(已有账号也能开启找回);
  「忘记密码」发一次性重置链接(1 小时过期、用后即废),重置后踢掉该账号所有
  会话。发信用标准 SMTP,配置无关——本机 25 直投或外部中转只看环境变量,未配
  SMTP 时把链接打到服务器日志(便于自托管起步与本地调试)。
- 首次上手改选「成为什么」:新用户首屏从选单科入门,升级为选职业目标(6 条
  「成为 X」路线,另留「只想学某一科 / 随便逛逛」兜底)。选定后目标存库,推荐卡
  顶部亮出目标、「下一步」按目标路线的课程顺序推进,地图自动定位到第一关。
- 「成为 X」学习路线扩到 6 条:新增成为游戏开发者、安全工程师、后端工程师
  (原有 AI 工程师 / 全栈 / 数据科学家),都用真实课程编排跨学科里程碑。
- 路径指引:地图顶部新增「继续学习 / 下一步」推荐卡(接上次没看完的一集 + 推荐首门
  可学未学的课),治「120 门课不知道先学什么」。

### 工程
- 包管理迁移到 pnpm(workspace 骨架):npm→pnpm,加 `pnpm-workspace.yaml`(留
  `packages/*` 口子供以后拆包)、`.npmrc`(node-linker=hoisted,贴近 npm 布局、
  最省心)。CI / Dockerfile / release.sh / 文档 / 本地启动配置全部改用 pnpm;
  原生模块(better-sqlite3 等)经 `onlyBuiltDependencies` 放行构建。
- 「成为 X」跨学科学习路线:新增 `/roadmaps`——成为 AI 工程师 / 全栈工程师 / 数据科学家
  三条职业路线,把数学→编程→AI 的课程串成里程碑清单,每门带学科徽标与锁/进度。
- 分享拉新:分享链接带 `?ref=<用户id>`,新用户经此注册记归因(`users.referred_by`),
  为后续邀请奖励打底。
- 成效评估升级(复习变「掌握引擎」):今日复习改为**服务端权威判分**(不再下发答案、
  不信客户端,防读答案自动全对);熟练的短术语卡升级成**填空·主动回忆**(看定义打
  术语,双语术语打任一片段即可,模糊判分容错拼写/大小写/标点);奖励绑定正确率——
  **答对才给 XP**(填空更多)、复习日奖励随正确率缩放、正确率≥80% 额外给金币(金币
  首次与掌握挂钩);课程页新增**掌握度进度条**(按复习卡长期留存派生,量的是记住多少
  而非看了多久)。看视频奖励不变。
- 新用户上手引导:首次进 `/play` 弹欢迎浮层——从 6 个学科方向(编程启程/数学直觉/
  物理启程/智能启程/影读启声/溯古华夏,各一条 basic 入门线)选一个 → 自动排好
  起步路线并高亮第一关 → 即领启程礼包(+200 金币)。零进度新人首屏先收起重系统
  (试炼/商店/背包/工坊),拿到 XP 后自动全部出现。引导只弹一次(`user_state.onboarded_at` 幂等)。
- 内容:补齐物理与 AI 的入门线,都用零前置、不用公式的科普片作入口——物理《宇宙的
  奇迹》(Brian Cox)、AI《AlphaGo》(Netflix)。新增「物理启程」(科普→经典力学)、
  「智能启程」(AlphaGo→神经网络的本质→机器学习)两条 basic 路线,使 6 大学科都有
  人人可入的地基线。(需要数学的正课仍保留前置,放在后面的章节,先建直觉再补数学。)
- 视频容错:课程可配备用搬运稿件(多分 P 课写 `source.mirrors`、合集课写
  `episode.mirrors`),主源被下架/风控解析不出流时,播放器按序自动切到备源。
- 「视频不见了」反馈:主源与全部备源都挂时,失败页给出反馈按钮(打不开/内容不对/
  其他),一键落库供运营换源。
- 独立管理端(运营后台):独立子域 `admin.<域名>` + 独立管理员账户登录 + 独立
  可安装 PWA,与学员端彻底分开(同一容器,middleware 按 host 路由到 `/console`)。
  功能:用户搜索与详情、调整进度 / 强制解锁(补前置进度)/ 数值(金币·XP·护盾·
  道具)、处理视频失效反馈;改动写 `admin_audit` 留痕。首启播种默认账户 `admin`
  (`ADMIN_INITIAL_PASSWORD` 或弱口令 + 强制首登改密)。Caddyfile 双 vhost。
- 视频失效探活 `npm run check:videos`:离线扫全站课程的 bilibili 稿件,报告
  确证失效且无备源的、该换主源的;三态判定不把风控限速误当失效。
- 每周定时 CI(`video-health.yml`)游客态跑探活,发现确证失效自动开/更 issue。

## [0.1.0] - 2026-08-08

首个正式版:必学堂——把公开课做成闯关游戏的自学平台。以下为本版已具备的能力。

### 平台与账号
- Next.js 16 + TypeScript + Tailwind v4;SQLite(better-sqlite3)+ Drizzle ORM,启动时自动迁移。
- 自建认证与会话;独立注册(可跳过 B 站登录)。
- bilibili 账号绑定;头像上传裁切 / 同步 B 站头像 / 默认 logo;昵称与密码修改。
- PWA:可添加到主屏当 App;双主题(自动 + 手动切换)。

### 内容体系
- 课程 schema + 加载器 + 校验脚本;从 csdiy.wiki 批量收录百余门公开课(计算机/数学/物理/AI/英语/历史)。
- AI 视频分析管线:逐集知识点 + 术语 + 时间轴(analyze-course 技能),卷宗按集动态解锁。
- 学习路线按学科与难度分级;课程/路线的解锁前置机制。

### 游戏化玩法
- 可行走的游戏大厅 + 多邻国式关卡地图:一门课拆成「看视频 / 阶段测验 / 宝箱」多节点,进度环 + 吸顶课程条。
- XP / 等级 / 角色面板;单集时长计分,经验药水(按集数与全局计时)。
- 遗物装备系统:背包、三合一融合、诅咒遗物(高风险高收益)、属性加成接入答题。
- 商店、每日任务、生命与护盾(以撒式)、连续学习防疲劳提醒。
- 题库 + 四选一答题会话;无限限时试炼、幽灵对战与排位。
- 全屏庆祝动效(Lottie / CSS),分享图(双模式 + 二维码 + 社交平台)。

### 记忆与复习
- 间隔重复(SM-2)复习卡:完成一集自动入卡,今日复习结算 + streak 连胜(可冻结)。
- 出题质量:干扰项「同课优先→同学科→兜底」,相关而易混;复习卡带「课程·第 N 集」上下文。

### 视频播放器
- 自研 bilibili DASH 代理 + 服务端 MPD 合成;ArtPlayer 播放器,AVC 优先、清晰度切换、弹幕。
- CC 字幕:双语主/副、按轨道毫秒偏移(众包默认值)、样式;章节跳转与碎片化分段;章节缩略图。
- 视频笔记(时间戳 + Markdown 编辑器,工具栏 + 预览)。

### 英语影子跟读
- 三条分级线(影读启声 / 循声 / 化境),线性解锁;单元=范围,内含多个「跟读」段节点 + 宝箱。
- 逐句练习闭环(盲听→精听→跟读→录音对比→复述),句子来自真实字幕轨、逐句时间对齐。
- 纯前端发音打分(Meyda MFCC + pitchfinder + DTW),原声/录音双波形对照,保音调变速。
- 选源与生成管线(WBI 签名解锁人工字幕轨、纯音频检测)。

### 历史学科线
- 三条分级纪录片路线:溯古华夏(中华文明五千年)/ 近代风云(辛亥到护法)/ 大国之路(大国崛起),按朝代覆盖,各课带知识点卷宗。

### 实验室与工坊(桌面端)
- nand2tetris 实验室:Hack 汇编器、VM 翻译器、Jack 编译器与完整 OS、WebGL 屏。
- 数学工坊;术语中英对照表。

### 工程与运维
- Docker + Caddy 自动 HTTPS;GitHub Actions CI(lint/test/typecheck/内容校验 + 镜像冒烟)。
- CI 出镜像推 GHCR + 服务器免 build 拉取部署;语义化版本 tag 触发发布 + 本 CHANGELOG + GitHub Release。

### 近期修复
- 跟读首句播放不跳转(等 DASH seek 就位再 play)。
- dashjs CMCD 遥测对相对 BaseURL 构造 URL 报错刷屏(两个播放器关闭 CMCD)。
- 设置页新增「关于」板块(GitHub 仓库 / 提 Issue / Star)。
